---
effort: m
id: TKT-014
kind: enhancement
priority: medium
status: done
title: Add multi-select widget support
type: ticket
---

## Description

Implement multi-select widget for list-typed enum properties in the data entry UI

- Auto-detect widget from metamodel `list: true` property
- Render as badge list in list, kanban, view, and entity detail pages
- Unify `Value`/`Values` fields into single `[]string` in display structs
- Add `toStringSlice` template helper for entity detail rendering
- Add `GetAttributeStrings` method to Entity model
- Extract `handleCreate` into focused helper functions

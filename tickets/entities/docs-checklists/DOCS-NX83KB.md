---
id: DOCS-NX83KB
type: docs-checklist
title: 'Documentation: CalVer releases (vYY.M.BUILD) with an automated tag cutter'
status: done
---

## Code Documentation

- [x] Comments where logic isn't obvious — the script header explains why this
format and not openvwr's; both workflows carry comments on the GITHUB_TOKEN /
`on: push` interaction
- [x] ~~Function/type docs if public API~~ (N/A: no Go API change; a shell
script and two workflow files)

## Project Documentation

- [x] README updated — N/A checked: the README does not document the release
process, which lives entirely in `docs/releasing.md`
- [x] ~~CLAUDE.md updated~~ (N/A: no new code pattern; the release process is
not covered by CLAUDE.md)
- [x] ~~Help text accurate~~ (N/A: no CLI change — `scripts/generate-version-tag.sh`
is a CI helper, and it does carry a `--help` usage line)

`docs/releasing.md` is rewritten: the CalVer scheme with examples, the two
constraints that determined the format (GoReleaser semver, MSI 255 cap), why
openvwr's `vYYYYMMDD` was not reused verbatim, the app-token tag-push trigger,
and updated cut/verify instructions.

## External Documentation

- [x] ~~Changelog entry added~~ (N/A: the changelog is generated by GoReleaser
from conventional-commit subjects — see `changelog:` in `.goreleaser.yaml`; this
commit is `ci:` and is filtered out by design)
- [x] ~~API docs updated~~ (N/A: no API change)

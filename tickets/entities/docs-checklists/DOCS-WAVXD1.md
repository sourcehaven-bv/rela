---
id: DOCS-WAVXD1
type: docs-checklist
title: 'Docs: Worlds: metamodel declaration, resolver, pushdown, selection (Step 2)'
status: done
---

<!-- @managed: claude-workflow v1 -->

## Code Documentation

- [x] Comments where logic isn't obvious — the load-bearing ones: why the
      fs/mem index sorts by the `(bare id, face)` TUPLE rather than the
      joined string; why the keyset stays OUTSIDE pgstore's `DISTINCT ON`;
      why `worldQuery := *rqr.Query` must be a copy (the ACL layer may
      cache a `ReadQueryResult` per principal, so in-place mutation is a
      cross-request scope bleed); why the SQL alias is a parameter applied
      at generation rather than a rewrite of generated output. The 26
      remaining pgstore `face = ''` literals are annotated under a
      three-category taxonomy, including the three traps.
- [x] Function/type docs if public API — package docs for `internal/worlds`
      (pure compiler) and `internal/worldreader` (runtime). The latter
      states guard rule 1 and why resolve-then-gate is the fixed order
      (an existence oracle otherwise). `store.WorldScope`,
      `store.Fallback` and `worldreader.Resolved` each document why their
      zero values mean what they mean.

## Project Documentation

- [x] README updated (if applicable) — regenerated by `just docs`; the
      worlds content lives in the metamodel guide rather than the README.
- [x] ~~CLAUDE.md updated (if new patterns)~~ (N/A: no new cross-cutting
      pattern. Worlds follow the existing decorator + wiring-site
      conventions CLAUDE.md already documents (DEC-ZBI39P), and the
      package-level rules live in the packages' own doc comments.)
- [x] ~~Help text accurate (if CLI changes)~~ (N/A: no CLI surface. Q10
      decided there is no `--world` flag and no `?world=` parameter —
      surfaces are wiring-bound. Request-level selection ships with its
      own grant check in Step 3.)

## External Documentation

- [x] ~~Changelog entry added~~ (N/A: this repo has no CHANGELOG file;
      user-facing documentation is generated from `docs-project/`.)
- [x] API docs updated (if applicable) — `docs-project/entities/guides/
      GUIDE-metamodel.md` documents `faces:`, `worlds:`,
      `select`/`overrides`/`otherwise`/`edits`, the three resolution
      rules, the reserved `default` world, and face-name grammar
      (PR-A); `docs/` regenerated with `just docs` and never edited
      directly. PR-D added a **"What worlds do NOT do yet"** section
      covering the two limits an operator will actually hit — no
      per-request world selection, and no search on a world-bound surface
      — each with its reason. Verified at PR-D that the PR-A text still
      describes shipped behaviour.

---
id: DOCS-SB55PN
type: docs-checklist
title: 'Documentation: lua bounded read API shape (stage 1)'
status: done
---

## Code Documentation

- [x] Comments where logic isn't obvious — `readopts.go` documents why `limit = 0`
is an error rather than "unbounded" (an unbounded read is the failure mode this
ticket removes, so a caller asking for it should hear about it); `pushdown.go`
records that the `AllowAll` branch still redacts, which is the #1188 defect class
- [x] Function/type docs if public API — the new `internal/lua` option-parsing
helpers and `internal/visibility.Pushdown` carry godoc naming the ceiling,
clamping behaviour, and the fail-closed contract on scope-composition errors

## Project Documentation

- [x] README updated — N/A checked: the README does not document Lua scripting,
which lives in `docs/lua-scripting.md`
- [x] ~~CLAUDE.md updated~~ (N/A: no new code pattern for contributors — this
tightens an existing API surface rather than introducing a convention)
- [x] Help text accurate — no CLI surface changed; the Lua API is documented
rather than exposed through flags

`docs/lua-scripting.md` and `docs-project/entities/guides/GUIDE-lua-scripting.md`
both gain the uniform read-options section: the `limit` / ceiling semantics,
`limit = 0` as an error, unknown option keys rejected rather than ignored, and
that iterator errors now raise instead of returning zero rows.

## External Documentation

- [x] ~~Changelog entry added~~ (N/A: the changelog is generated by GoReleaser
from conventional-commit subjects — see `changelog:` in `.goreleaser.yaml`)
- [x] API docs updated — the Lua read API is the API surface here, and both the
user-facing doc and the project guide are updated in this PR

---
id: IMPL-2637G9
type: implementation-checklist
title: 'Implementation: sqlite history attributes create/update versions to version-sweep instead of the editor'
status: done
---

<!-- @managed: claude-workflow v1 -->

## Development

- [x] Unit tests written for new code
- [x] Integration tests written (test full flow, not just units)
- [x] Happy path implemented
- [x] Edge cases from planning handled
- [x] Error handling in place (errors surfaced, not swallowed)

## Test Quality

- [x] Using fixture builders or factories for test data
- [x] No hardcoded values in assertions when object is in scope
- [x] Only specifying values that matter for the test
- [x] Interpolated values constructed from objects, not hardcoded
- [x] Property comparisons use original object, not hardcoded strings

## Manual Verification

- [x] Feature manually tested end-to-end
- [x] Each acceptance criterion verified with test scenario from planning
- [x] Edge cases manually verified

**Verification Evidence:**

- storetest.RunSweepAttributionTests passes on sqlitestore and on pgstore
(local postgres). With the pre-fix sqlite sweep it fails: expected "alice",
actual "".
- TestSQLiteSweptVersionCreditsThePrincipal (appbuild, sqlite tag) drives a
principal-stamped write through the assembled entitymanager and reads the swept
version back. Fails pre-fix, passes post-fix.
- Manual: a sqlite build driving `rela mcp` over stdio. New create and update
versions show `jeroen (mcp)`. An existing v6 database upgraded to v7 on open;
its older versions keep `version-sweep`.
- Edge cases: an unattributed write after an attributed one falls back to
version-sweep rather than inheriting the previous editor; relations behave the
same as entities; the v7 rung is idempotent (pragma-probed ALTER) and the
fresh-vs-migrated shape test passes.

## Quality

- [x] Code follows project patterns (check similar code)
- [x] Checked for DRY opportunities — repeated literals, expressions, or
patterns extracted to a helper / constant / type where it sharpens the contract
(don't extract for its own sake; CLAUDE.md "three similar lines is better than a
premature abstraction" still holds)
- [x] No security issues introduced
- [x] No silent failures (errors logged AND returned)
- [x] No debug code left behind

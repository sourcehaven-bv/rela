---
id: RR-609PG2
type: review-response
title: CLI commands accept ID@face at the existence check but not after it
finding: rela delete / acl can resolved X@draft through GetEntityAt, then counted relations and called DeleteEntity with the raw address, so the cascade guard was skipped and a confirmed delete failed. rela show listed relations by the typed address and showed none.
severity: significant
resolution: delete and acl can reverted to bare-id GetEntity, so X@draft is a clean not-found on every backend. show queries relations by the row's bare id and restricts outgoing edges to the addressed face; pinned by TestShowFaceAddress. MCP delete reads the raw store, so it already refuses the address.
status: addressed
---

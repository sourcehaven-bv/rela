---
id: RR-1659OA
type: review-response
title: readquery.go group expansion is safe only transitively via walkMembers; no guard test or seam comment
finding: 'readquery.go:54-57 is a second consumer of group membership. It is safe because it uses globals.Members (pre-computed by walkMembers, which uses the accessor) rather than naming the relation itself. But nothing pins this: a future refactor that pushed group expansion into a store.RelationPredicate with a hard-coded "member-of" (the exact shape store/graphquery.go documents) would silently bypass the single-guard invariant and re-introduce a hard-coded relation name. Mitigation: add a comment at readquery.go:57 explaining that Endpoints: globals.Members is the deliberate seam that inherits the configured membership relation, so the next person does not inline it. A guard test would be ideal but the comment is the minimum.'
severity: significant
resolution: Added a seam comment at readquery.go HasInbound.Endpoints explaining that globals.Members is already expanded by walkMembers over the configured membership relation, so this read path inherits it for free, and warning against inlining the membership relation as another RelationPredicate (which would re-hardcode member-of and bypass the accessor guard). Kept as comment rather than test since the transitive coverage is real and a guard test would be brittle.
status: addressed
---

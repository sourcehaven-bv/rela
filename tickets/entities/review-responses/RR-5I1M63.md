---
id: RR-5I1M63
type: review-response
title: Load-time where check validates the property name but not that the operator is legal for its type
finding: Code review, leverage item. validateConstraintWhere checks only that the property name is declared. With target_type set the PropertyDef is in hand at load, so the operator could be checked too — filter.validateOperatorForType rejects < and > on a string property, but only at runtime, where it becomes a MatchAll error and gets fail-closed-counted. Checking it at load would close the other half of the same hole for about five lines.
severity: minor
resolution: 'Not changed. Worth filing separately: it is a real improvement and the reviewer is right that the information is available.'
reason: 'Deferred as genuinely out of scope rather than hard. The ticket''s load-time item was specifically about the two NEW keys and the silent-no-op class they introduce; operator/type mismatch is a pre-existing property of every `where:` clause on every shipped gate, unchanged by this work. Fixing it here would mean either applying it only when target_type is set (a third inconsistency in when where: is validated, which is what RR-M2XRW2 was about avoiding) or reworking validation for all clauses — a bigger change that deserves its own ticket and its own regression run against the real schemas.'
status: deferred
---

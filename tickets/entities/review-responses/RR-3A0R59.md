---
id: RR-3A0R59
type: review-response
title: Node registered per-editor, recreating the drift editorPreset.ts exists to prevent
finding: '`relaCommentRemarkPlugin` and `relaCommentNode` were registered separately in MilkdownEditor.vue and relaEditor.ts, with a duplicated comment in each saying the two must match. `editorPreset.ts`''s own doc calls that exact shape the drift TKT-D2JML7 was spent removing. The registration decides whether a comment serializes through the preset''s `html` node or the chip, so by the file''s stated criterion — anything deciding what bytes an editor writes — it belongs in the shared preset.'
severity: significant
resolution: 'Added `RELA_OUTPUT_NODES` to editorPreset.ts holding the plugin and node together, and both editors now `.use(RELA_OUTPUT_NODES)`. The export''s doc states why the pair must load together (the node alone loses the match) and why entityRefNode was deliberately NOT moved in the same change: the two editors resolve its titles differently, so relocating it is its own work rather than a drive-by.'
status: addressed
---

I initially followed `entityRefNode`'s per-editor registration as precedent. The
reviewer's counter is correct: that is a pre-existing instance of the same
smell, not a licence to add another. The distinction `editorPreset.ts` actually
draws is output vs. experience, and this is output.

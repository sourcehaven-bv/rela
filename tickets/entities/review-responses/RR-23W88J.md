---
id: RR-23W88J
type: review-response
title: Adapters also differ in field-SELECTION policy, not just int/date value type
finding: 'Beyond int/date (RR-TBG91), affordances.entityRecordType and predicatefns.EntityRecordType differ in WHICH fields they declare: (1) affordances OMITS unsupported types via (Type,bool)=false (file->omitted, unknown/custom-non-enum->omitted) so a predicate referencing them fails at COMPILE (''unknown variable'', DR-C2 intentional); predicatefns maps file/unknown->StringType (declares them). (2) affordances always adds id+type pseudo-fields; predicatefns does not. (3) affordances resolves custom enum-like types to String via meta.Types lookup; predicatefns has no meta and defaults everything to String. So ''just call EntityRecordType'' would change affordances'' compile-time field surface (file predicates start compiling+evaluating-as-string instead of erroring). FIX: don''t replace affordances.entityRecordType wholesale. Keep affordances'' omit-policy + id/type pseudofields + meta-aware custom-type resolution; delegate ONLY the scalar int/date/bool mapping to a shared helper so int->Int/date->Date is the single source of truth. Either extend predicatefns.scalarPredicateType to expose an (ok bool) + meta-aware variant, or have affordances call predicate.IntType/DateTypeWithLayout directly for those two cases. The value-binder swap (NewInt/NewDate) is unchanged by this.'
severity: significant
resolution: 'Did NOT replace affordances.entityRecordType wholesale. The scalar type CHOICE (int->Int, date->DateTypeWithLayout, bool->Bool, string/enum/custom->String, file/unknown->omit) now lives once in predicatefns.ScalarType/ScalarTypeForProp (meta-aware, (Type,ok) omit semantics). affordances.propertyPredicateType delegates to ScalarTypeForProp but keeps its own policy: list wrapping + id/type pseudo-fields (entityRecordType) + omit-on-unmodelled. predicatefns.EntityRecordType also uses the shared helper (and now takes *Metamodel for custom-type resolution + omits unmodelled types, matching affordances'' field-selection). So the int/date choice is single-source; field-selection policy is preserved. Verified: full affordances suite + dataentry (ACL read path) green.'
status: addressed
---

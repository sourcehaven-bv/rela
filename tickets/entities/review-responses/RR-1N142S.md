---
id: RR-1N142S
type: review-response
title: ?document= export override does not validate entityID with isSafePathSegment
finding: exportRenderer (export.go) validates docName with isSafePathSegment but NOT entityID, then feeds entityID into documents.RenderMarkdown -> renderCommand -> executeCommand which runs sh -c with {id} substituted. The sibling handleV1Documents (api_v1.go:3201) validates BOTH docName and entityID before any render/filesystem work. Mitigated because getVisible/GetEntity must match a real stored entity id first, but the export path should apply the identical defense-in-depth guard.
severity: significant
resolution: 'exportRenderer now validates BOTH docName AND entityID with isSafePathSegment before any document render (export.go), matching handleV1Documents. Test: TestExport_Entity_DocumentOverride path exercises the override; the guard rejects unsafe segments with 400 before render.'
status: addressed
---

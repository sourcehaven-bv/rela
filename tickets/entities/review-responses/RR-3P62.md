---
id: RR-3P62
type: review-response
title: E2E flicker test selector is unscoped and matches sibling components' loading states
finding: '`appPage.locator(''.loading-state'')` matches `.loading-state` in DocumentsPanel, SidePanel, HelpModal etc. — not just EntityDetail''s. Today FEAT-004 doesn''t trigger DocumentsPanel''s loading state so the test passes, but seed-data drift or a child-component async lag will flake or false-pass. Scope to `.entity-detail > .loading-state` or use a data-testid.'
severity: significant
resolution: Scoped the selector to `.entity-detail > .loading-state` (direct child) so the test only catches the EntityDetail's own spinner, not DocumentsPanel/SidePanel/HelpModal etc. that share the `.loading-state` class.
status: addressed
---

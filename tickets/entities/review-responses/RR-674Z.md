---
id: RR-674Z
type: review-response
title: Misleading recover() comment in Compile
finding: 'compile.go:60-65: the deferred recover() comment claims it catches gopher-lua parser panics. It can''t — parse.Parse has its own recover() (verified in parse/lexer.go:471-475) that converts panics to errors before they propagate. The only panic the predicate-level recover could actually catch is a panic in the walker, which currently can''t happen. Rewrite the comment as ''Belt-and-suspenders: parse.Parse already converts its own internal panics. This recover catches any panic added by future walker changes or a hypothetical regression where gopher-lua removes its top-level recover.'' Keep the code; fix the documentation.'
severity: significant
resolution: 'Rewrote the recover() comment in compile.go to accurately describe its purpose: ''Belt-and-suspenders: parse.Parse already converts its own internal yacc-driven panics into typed errors, so this recover is unreachable today. It exists for two contingencies: (a) a panic added by future walker changes, and (b) a hypothetical regression where gopher-lua removes its top-level recover.'' Code unchanged.'
status: addressed
---

---
id: RR-1O02
type: review-response
title: DarkMode error messages mention `disabled` keyword that was never valid YAML
finding: |-
    internal/dataentryconfig/palette.go: the `UnmarshalYAML` test case `disabled string is rejected (legacy)` (palette_test.go:95) implies `disabled` was an old keyword. But the previous code (per the ticket description) used `auto` and `false`, never the literal string `disabled`. The error message at line 68 / 122 is correct (`use either `false` or an explicit palette object`) but the test's '(legacy)' label is misleading documentation. Drop the `(legacy)` from that test name or change it to `(invalid string)` so future readers don't think `disabled` was once a real value.

    Also: `invalid dark mode `true`` in the error message uses backticks inside a Go string with backticks — need to check the rendering. Looks like it's a regular `"..."` string, so the backticks are literal in the user-facing error. Fine but a bit ugly; consider plain quotes for consistency with other Go error messages in the codebase.
severity: nit
resolution: Renamed test case from `disabled string is rejected (legacy)` to `disabled string is rejected` in palette_test.go — the `(legacy)` qualifier was misleading because `disabled` was never valid YAML for this field.
status: addressed
---

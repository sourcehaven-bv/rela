---
id: RR-GVHEIK
type: review-response
title: No per-subscriber gating seam exists — the broker is principal-blind; per-event gating requires re-architecting the fan-out into handleSSE
finding: 'CORRECTED severity (user diagnosis 2026-06-13): NOT a missing-principal problem — the principal IS reachable where needed. handleSSE (watcher.go:310) holds the http.Request, attachACLRequest already wraps the SSE route (router.go:118), so r.Context() carries the principal + readGate in the per-connection loop today. The actual issue is narrower and is a data-flow/plumbing change, not a re-architecture: broadcastEntityEvent (watcher.go:74) PRE-RENDERS the final {type,id} JSON into sseEvent.Data in the principal-less pumpStoreEvents goroutine, BEFORE fan-out. So the per-connection gate+cacheId (both principal-dependent) would have to re-parse that JSON. Fix: change the channel payload for ENTITY events from a pre-rendered sseEvent to a structured {Op,Type,ID}; move gate+cacheId+render INTO the handleSSE per-connection loop (where the principal already is). Keep non-entity events (refresh, git:status) on the existing pre-rendered string path — they have no principal-specific content; the broker forks: structured for entity ops, plain string for the rest (easy to get subtly wrong, hence still significant not trivial). The genuinely-hard part is NOT this plumbing — it''s making the per-connection gate CHEAP (RR-2LTVUL, remains critical). Pin: two simultaneous connections, different principals, different frames for the same store event.'
severity: significant
resolution: 'Confirmed plumbing, not re-architecture (user diagnosis). Final design: broker carries the entity event''s `{type}` (not a pre-rendered frame); handleSSE per-connection loop — which already holds r.Context()/principal via attachACLRequest — applies ReadQuery(type) before writing. Non-entity events stay on the existing path. AC1/AC2 (two principals get different nudges for the same store event) pin it. Even simpler than the cacheId-era version because there''s no per-entity render, just a type gate.'
status: addressed
---

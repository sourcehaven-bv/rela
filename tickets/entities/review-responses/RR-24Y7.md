---
id: RR-24Y7
type: review-response
title: PATCH path doesn't pass If-Match etag; concurrent toggles last-write-wins
finding: updateEntity called without etag arg; legacy endpoint also had no etag so this is parity. ViewResponse doesn't currently expose an etag, so plumbing one through is meaningful work. Either leave a comment justifying intentional skip, or thread etag from the previous PATCH/fetch response.
severity: nit
resolution: 'Added an inline comment justifying the absence of If-Match: ''toggles are commutative-ish (last-write-wins is acceptable for two users racing the same checkbox) and the ViewResponse doesn''t currently surface an ETag for us to thread through. Matches the prior /api/toggle-checkbox behavior.'' Threading an ETag end-to-end is a separate ViewResponse-shape change worthy of its own ticket.'
status: addressed
---

---
id: RR-290V
type: review-response
title: Add frame-ancestors 'none' to logo CSP for defense in depth
finding: 'GET /api/v1/_theme/logo response sets Content-Security-Policy: sandbox (handlers_theme.go:54), but frame-ancestors is not specified. Same-origin middleware already rejects cross-origin embedders, but defense in depth is cheap: append `frame-ancestors ''none''` (or set X-Frame-Options: DENY) so a malicious page cannot frame the SVG even if origin checks are loosened later.'
severity: significant
resolution: 'handlers_theme.go GET handler now sets `Content-Security-Policy: sandbox; frame-ancestors ''none''` and `X-Frame-Options: DENY`. New TestThemeLogo_SVGGetHeaders asserts both headers on the SVG-served response.'
status: addressed
---

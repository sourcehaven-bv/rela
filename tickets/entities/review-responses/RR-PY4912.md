---
id: RR-PY4912
type: review-response
title: Engine.Probe() is dead code; promised startup probe not wired
finding: transform.Engine.Probe godoc says the composition root calls it at startup so a missing converter warns rather than failing per-export, but it is called from nowhere in non-test code (attachments DO wire a startup probe). A host missing pandoc gets no warning; the first export 500s. Either wire the probe at composition or delete Probe and fix the docstring so code and docs agree.
severity: minor
resolution: probeTransformCommands(meta) wired in App construction (app.go) right after probeAttachmentCommands; it builds the engine and calls Engine.Probe(), logging a warning per configured transform whose binary is missing. Engine.Probe is now live (docstring accurate). A host without pandoc gets a boot warning instead of a first-export 500.
status: addressed
---

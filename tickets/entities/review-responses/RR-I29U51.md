---
id: RR-I29U51
type: review-response
title: Rebase spliced the command-gating docs into the MCP section
finding: |-
    The new command-gating content in docs/server-security.md was spliced directly onto the end of the 'Remote MCP exposes every tool' section's final bullet, with no blank line and no heading of its own. It rendered as a continuation of the MCP gap list — the table about command execution appeared to be about MCP batch dispatch.

    A visible rebase artifact: this branch was rebased across ~400 upstream commits and the surrounding section had been rewritten upstream. The example command in the same block was also wrong: `rela-server --bind 0.0.0.0:8080` passes a host:port to a flag that takes a bare host (net.ParseIP rejects it, so it fails closed, but the documented escape hatch did not work as written and the listener address was wrong too). The fenced block also had no language, which markdownlint MD040 rejects.
severity: minor
resolution: |-
    Moved the whole block under the existing '### Configured commands are remote-code-execution by design' heading (docs/server-security.md:543), immediately after the paragraph it belongs to, and verified the MCP section is intact and unbroken afterward. Corrected the example to `rela-server --bind 0.0.0.0 --port 8080 --allow-unauthenticated-commands`, matching the convention already used elsewhere in the same document, and tagged the fence `sh`.

    Note: one reviewer reported the GUIDE-acl-security.md anchor link as broken. Verified it is NOT — the target heading exists and the link resolves; the actual defect was only the misplacement.
status: addressed
---

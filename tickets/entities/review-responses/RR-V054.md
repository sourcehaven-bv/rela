---
id: RR-V054
type: review-response
title: checkboxToggle.test.ts splits cases vs errors in inconsistent shape
finding: Two error cases live outside the `cases` array; Go test had them as table rows with `wantErr`. Reproduce the unified shape or split cleanly into `cases` and `errorCases`.
severity: nit
resolution: Split tests into `okCases` and `errCases` typed arrays with consistent shape. Both arrays iterated cleanly; the error block now lives in its own describe('errors'). Also added coverage for the GFM-spec edge case `- [ ]nospace` (correctly rejected, matching marked).
status: addressed
---

---
id: RR-7081BE
type: review-response
title: 'PR-B: rename indexed renamedStates[0] without a length check - panic on a racing delete'
finding: RenameEntity read newType from renamedStates[0] after the family UPDATE..RETURNING; a family deleted between the (non-locking) existence probe and the UPDATE returns zero rows, making the index an out-of-range panic instead of an error. The pre-refactor single-row scan had ErrNoRows handling that the slice refactor dropped — while the sibling nil-check on `renamed` three lines later shows the empty case was considered in one place and not the other.
severity: critical
resolution: len(renamedStates) == 0 now returns store.ErrNotFound before the index, with the race explained in a comment. Belt-and-braces alongside the FOR SHARE fix.
status: addressed
---

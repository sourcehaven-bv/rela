---
id: RR-44DP60
type: review-response
title: Unprimed or unpersisted rows read related() as false
finding: 'Answers.For returned ans[rowID]; a row outside the answered set (includes; neighbours; another type with the same spec key; an unpersisted create candidate with id "") read as no match; so `not related(...)` in an ACL when: passed.'
severity: critical
resolution: Answers now records the ids it was asked about; For errors for any other id and for an empty id; the ACL memo is keyed by (type; id; spec) and a miss binds that row live. Tests TestAnswersFor_RefusesUnaskedRow; TestBinder_EmptyIDIsAnError; ACL test planned for an unprimed row under not related.
status: addressed
---

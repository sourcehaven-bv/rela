---
id: RR-4JF5U3
type: review-response
title: 'Relations: reuse FilterRelations (no fourth endpoint-visibility impl), expose no edge meta, run cycle detection on the visible subtree'
finding: 'Three relation-axis rules. (1) There are already three implementations of the both-endpoints-visible rule (PolicyReader.FilterRelations policyreader.go:129; hand-rolled PermitsRead(from)&&PermitsRead(to) in relation_read_handler.go:113-118 and relation_history_handler.go:156-161; inline batching in matchRelationFilter api_v1.go:559-565). The hierarchy walk must not add a fourth — use FilterRelations; if its N-load cost profile (one GetEntity per distinct endpoint per call, no cache, no relation pushdown — luareader.go:36-40 calls it ''an N-load amplification'') forces something better, fix the shared one. (2) Relation META is not redacted on the visibility path (appbuild.go:490-498, TKT-0RBFN0) — meta redaction lives only in the dataentry serializer. GanttNode must therefore expose NO edge properties in v1; a future ordering/weight hint must route through affordances.RelationFieldVerdicts. (3) VisibleTracer.HasCycle reports cycles whose loop passes through hidden nodes (tracer.go:255-259) — an accepted one-bit residual THERE, but on_cycle: error would make it a load-visible oracle here. Detect cycles on the already-gated subtree, consistent with gate-then-fold. Bonus: the drop-subtree orphan policy is not a novel call — VisibleTracer.rebuild (tracer.go:104) already prunes the whole subtree at a hidden node; cite it as the settled precedent.'
severity: significant
resolution: 'Plan updated: endpoint visibility uses the shared PolicyReader.FilterRelations (no fourth both-endpoints implementation; if its N-load cost bites, fix the shared one); GanttNode exposes no relation properties in v1 (edge meta unredacted on the visibility path, TKT-0RBFN0); cycle detection runs on the already-gated subtree (AC6), avoiding the HasCycle-through-hidden-nodes oracle. VisibleTracer.rebuild cited as the settled drop-subtree precedent.'
status: addressed
---

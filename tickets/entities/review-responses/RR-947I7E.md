---
id: RR-947I7E
type: review-response
title: 'Stale overlap-window comment: cascade/rename now burn a seq block'
finding: 'listener.go catchUpOverlap comment claimed ''rela''s writes are short single-row operations''. That premise is now false: a cascade delete writes one tombstone per cascaded relation, and a rename re-keys + tombstones the entity and every incident relation — each consuming a fresh nextval, so one tx burns a contiguous block of seqs committing at once. A future reader trusting the stale premise could mis-reason about the overlap safety.'
severity: significant
resolution: Updated the catchUpOverlap comment to state that cascade delete and rename are multi-row seq-burns (bounded by an entity's relation count, tens not thousands), so the 100-overlap stays generous; the genuine break remains a bulk-import in one long tx (the documented xid8 upgrade trigger).
status: addressed
---

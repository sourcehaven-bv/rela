---
id: RR-VQQQ60
type: review-response
title: DynamicForm never seeds a merge base — lastSeenServer is empty on the main edit form
finding: 'The plan''s foundation ("lastSeenServer/lastSeenContent are ALREADY the correct merge base — no new state required") is FALSE for the primary edit surface. useAutoSave seeds the base only from opts.initialServerSnapshot (useAutoSave.ts:254-255) or an explicit recordServerSnapshot call (:244). Verified: SectionEditForm.vue:95 passes initialServerSnapshot and EntityDetail.vue:348 calls recordServerSnapshot, but DynamicForm.vue — the main entity edit form and the explicit-save target of Step 1 — does NEITHER. loadEntity() assigns formData/content directly without handing the entity to useAutoSave. So lastSeenServer={} and lastSeenContent='''' until the first PATCH response. A three-way merge with base='''' degenerates: diff3 sees ''both sides added the entire document'' → whole-file conflict on the FIRST 412, and properties get base=undefined for every key. AC3 (disjoint fields auto-resolve, no UI) fails on every DynamicForm session. FIX: wire initialServerSnapshot/recordServerSnapshot into DynamicForm, AND add an explicit baseRecorded sentinel — undefined is ambiguous between ''never seen'' and ''genuinely absent server-side''; the merge must refuse to run and fall back to current behavior when no base was recorded.'
severity: critical
status: addressed
resolution: 'Confirmed accurate and fixed in code. DynamicForm genuinely seeded no merge base: SectionEditForm passes initialServerSnapshot and EntityDetail calls recordServerSnapshot, but DynamicForm''s loadEntity() assigned the fetched entity straight into formData/content/relations without handing it to useAutoSave, leaving lastSeenServer empty on the main edit form. Split out and shipped as TKT-52OFC9 because it is a real defect independently of the conflict work -- with no baseline, no-op suppression cannot fire, so retyping a value identical to the stored one issued a pointless PATCH. The fix takes the snapshot at loadEntity''s existing fresh-server-state moment, BEFORE the spreads hand the entity to the form, and clones plus freezes it: had it aliased the form copy, later keystrokes would mutate the ''server'' side and a three-way merge would degenerate to two-way. Mutation-verified tests assert this via no-op suppression, including an edit-then-revert case. The finding''s second request is also kept: TKT-2VDVHF retains the explicit baseRecorded sentinel, since undefined is ambiguous between ''never seen server'' and ''genuinely absent server-side'', and the merge must refuse to run and fall back to current behaviour when no base was recorded. Under the new design an absent base now degrades to a single-field conflict rather than a whole-document one.'
---

---
id: RR-HQMQJB
type: review-response
title: 'rollup: cannot always be validated against the child type; needs a three-way policy'
finding: 'The plan says config load must reject rollup: naming a property absent from the child type. But determineTargetType (validate.go:1564-1591) returns "" for any relation with multiple to: types -- legal, non-error config, and common in the real schema (depends-on, verifies, caused-by). So there are three cases, not two: unknown bucket (hard error), known bucket with determinable type (validate the property), and known bucket with undeterminable type (cannot validate). The existing ambiguousWidgetSource/widgetSectionDef pair (validate.go:1146-1200) exists precisely to split these, treating the third as warning-only. The plan owes a stated policy per case. Related hazard: viewCollectionTypes (validate.go:1176) is a SECOND implementation of the bucket map whose godoc records that it already diverged once by omitting the entry bucket, silently skipping every source: entry section -- so a children: check added only to the inline map in validateViews would half-validate.'
severity: significant
resolution: 'Moot: rollup: is out of scope for this ticket (user decision -- the nested view itself was the request). With no rollup: key there is no property to validate against the child type, so the three-way policy question does not arise. The determineTargetType finding is still true and will apply if a rollup is added later; recorded on the deferred-rollup follow-up rather than lost.'
reason: 'Moot rather than rejected: the user descoped the rollup from this ticket, so there is no rollup: key left to validate against the child type and the three-way policy question does not arise here. The finding is verified and correct -- determineTargetType (validate.go:1564-1591) does return an empty string for multi-to: relations, which is legal config -- and it has been carried verbatim onto TKT-ZAD9PS (Per-parent rollup bar) as one of the two non-obvious problems that ticket must solve, complete with the ambiguousWidgetSource/widgetSectionDef precedent to follow. The related viewCollectionTypes divergence hazard is retained in this ticket''s plan, since the children: check still needs it.'
status: wont-fix
---

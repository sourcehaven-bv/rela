---
id: RR-8DGI17
type: review-response
title: 'The fix itself has zero test coverage: deleting the config bind leaves CI fully green'
finding: 'The entire point of this ticket is that clamdscan cannot read clamd.conf, so the bind must exist. Deleting the single line `binds = append(binds, cmdexec.DefaultScannerConfigs...)` from internal/attachment/cmdrunner.go reintroduces the P0 bug, and internal/attachment, internal/cmdexec and internal/metamodel ALL still pass. Independently reproduced: with the line removed, 0 references remain and the three packages report ok. TestDefaultScannerConfigsAreAbsoluteFiles only asserts the list LITERAL is well-formed; nothing asserts anything ever READS the list, so the wiring — which is the actual fix — is unguarded. This is precisely the silence that let the original bug ship: the next person to simplify the append chain deletes the configs line, gets green CI, and reships the bug that cost a VM afternoon to diagnose. Fix: assert the runner''s binds actually contain the default configs. cmdexec.Runner.extraReadOnly is already directly asserted at sandbox_test.go:246 within the package, so a cmdexec-side test is cheap; add an attachment-side TestCmdRunnerBindsScannerConfigs (via an exported accessor or export_test hook) so the wiring is pinned where it lives.'
severity: critical
resolution: 'Added TestCmdRunnerBindsScannerDefaults and TestCmdRunnerBindsOperatorPathsAlongsideDefaults in internal/attachment/cmdrunner_test.go, asserting the runner''s actual binds contain every DefaultScannerSockets AND DefaultScannerConfigs entry, plus that operator scan_sockets entries EXTEND rather than replace the defaults. Reachability came from a new cmdexec.Runner.ExtraReadOnly() accessor (returns a defensive copy; godoc''d as diagnostic/test-support only, explicitly not a can-I-run predicate). Verified by re-running the reviewer''s mutation: deleting the DefaultScannerConfigs append now FAILS both tests with `scanner config "/etc/clamav/clamd.conf" is not bound; clamdscan cannot find LocalSocket`, and passes again on restore. The fix can no longer be deleted silently.'
status: addressed
---

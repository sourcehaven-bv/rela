---
id: RR-02P03I
type: review-response
title: '''Freeze filter'' ignores 4 live consumers (dataentry views/feeds, lua, searchparser, analyze)'
finding: 'Plan scopes migration to automation+validation+CLI-list but filter.Parse/Match has more consumers unaddressed: dataentry/{views,feed_provider,helpers}.go (SPA view/feed where: clauses), lua/runtime.go:896 (script queries), search/searchparser/parser.go:67,84 (search query property clauses), cli/analyze.go + cli/filter_helpers.go. ''Freeze filter as transpiled alias'' + CLAUDE.md ''filter is the frozen legacy subset'' is misleading if ~4 subsystems still call filter.Match directly. FIX: add explicit ''consumers NOT migrated in Phase 2 (stay on filter.Match, unchanged)'' list to Scope-OUT naming these, to bound blast radius honestly and prevent assuming the frozen-alias story extends to the SPA.'
severity: significant
reason: 'Deferred to phase 2b (TKT-J4IR1G). This is a scope/docs task: naming the filter consumers (dataentry views/feeds, lua runtime, searchparser, cli/analyze) that stay on filter.Match and NOT claiming ''filter is fully frozen'' in CLAUDE.md. Phase 2a introduces no user-facing filter/predicate framing and touches no docs, so there is nothing to mis-state yet. The explicit not-migrated list is written into TKT-J4IR1G''s Scope-OUT and will land with the CLAUDE.md/docs updates in that PR.'
status: deferred
---

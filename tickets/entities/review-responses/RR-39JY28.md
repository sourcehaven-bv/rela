---
id: RR-39JY28
type: review-response
title: useListActions.test.ts mocked the pre-normalization rejection shape
finding: runAction mocks rejected with bare ScriptError envelopes, which never reach useListActions in production now that the interceptor wraps everything in ApiError; the tests passed only via getScriptError's bare-envelope fallback.
severity: minor
resolution: 'Mocks now reject with new ApiError(kind: ''script'', scriptError: envelope) via a wrapScriptError helper, matching the real boundary; the assertion still verifies the unwrapped envelope reaches scriptErrorStore.show.'
status: addressed
---

---
id: RR-F6182G
type: review-response
title: Start-time regression test never enters doExecuteTask — reverting the fix keeps tests green
finding: 'TestDoExecuteTask_recordsStartTimeNotCompletion builds its scheduler via newRetryTestScheduler, which installs executeTaskFunc; executeTask short-circuits to the override so doExecuteTask is never entered. The assertion validates the test double''s own success bookkeeping, not production code. Verified by mutation: reverting scheduler.go:283 to s.state.Tasks[task.Name] = s.now() (the exact pre-fix bug) leaves the full package green.'
severity: critical
resolution: 'Extracted recordSuccess on Scheduler; doExecuteTask and the test double now call the same code. Replaced the test with one driving the REAL doExecuteTask via newRealPathScheduler (real script.Engine, real .lua on disk, clock advancing between start and completion). Verified by re-running the mutation: reverting the stamp to s.now() now FAILS TestDoExecuteTask_recordsStartTimeNotCompletion.'
status: addressed
---

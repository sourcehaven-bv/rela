---
id: RR-S0XH7B
type: review-response
title: The fail-open guard in Compile was untested — the regression it prevents passed the suite
finding: 'worlds.Compile bails on grammar errors BEFORE compiling any world, with an eight-line comment explaining why: a type whose face names all failed the grammar has an EMPTY entry in the declared-face map, which compileWorld reads as rule 1 — ''declares no faces, contributes its default state in every world'' — the exact opposite of what a type declaring content states means. The reasoning and the fix are both correct, but NOTHING pinned it. The reviewer reverted the guard to the compile-then-report shape (collect errors, compile all worlds anyway, return Compiled{byName: byName} alongside joinErrors(errs)) — the precise fail-open the comment describes — and `go test ./internal/worlds/` passed clean. Every existing grammar test discards the Compiled return with `_`, so no test asserted that the value handed back alongside an error is empty. The comment even anticipates the future caller that would make this live (''even if a future caller wants a best-effort compile''), and that caller would have had no failing test to warn them. This was the one place in the PR where the prose was doing work the tests were not, and it was on the fail-open axis.'
severity: significant
resolution: 'Fixed in 322aa4b6 by adding TestCompile_GrammarErrorYieldsNoUsableScope. It asserts that after a grammar failure compiled.Names() is empty, Lookup("pub") returns ok=false, and — stating the dangerous shape positively — that the only reachable scope is the untouched default world, so a type whose face names did not parse can never be reached as a rule-1 (absent => default state) type. Mutation-tested: reverting Compile to the best-effort shape fails with ''a failed compile must not hand back usable worlds'' (Names = [pub]) and ''a world compiled from grammar-invalid faces must be unreachable''. No production change was needed — the guard was already correct; only the pin was missing. Found by code review.'
status: addressed
---

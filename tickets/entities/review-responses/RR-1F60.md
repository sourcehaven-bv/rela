---
id: RR-1F60
type: review-response
title: Plan does not consider WebSocket upgrade endpoints
finding: 'Quick verification: SSE is the only streaming primitive (no WebSocket). But the plan should explicitly call this out so a future WebSocket addition gets the same scrutiny. WebSockets famously bypass same-origin policy and need separate Origin checks at the upgrade handshake.'
severity: nit
resolution: 'Plan updated: docs/security.md notes that any future WebSocket endpoint must apply requireSameOrigin at the upgrade handshake, since the browser does not enforce same-origin for WS.'
status: addressed
---

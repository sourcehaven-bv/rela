---
id: RR-2775B7
type: review-response
title: AC 7's elevated half was unverified — the highest-consequence claim had no test
finding: 'AC 7 states the identity-scope refusal must hold ''from both the ordinary and elevated bindings''. No test exercised admin.create_relation with a face: grepping face_write_test.go for ''admin'' returned nothing, and acl_bypass_test.go contained no face references. RR-3NNWNK made the elevated path the reason requireRelationFaceFor had to live in entitymanager rather than in the binding — because authorizeAndAudit short-circuits under bypassACL, leaving the manager check as the only validation between a Lua string and store.RelationData.FromFace. That is the single highest-consequence claim in the change and nothing tested it end to end.'
severity: significant
resolution: 'Added runElevatedScript (wires the same mockManager as ElevatedManager so rela.bypass_acl registers) and TestElevatedCreateRelation_TakesAFace with two subtests: the face threads through to the manager boundary, and a malformed options table raises with zero manager calls. Asserts at the boundary rather than on a returned table because admin.create_relation pushes a bare boolean — the asymmetry the design review flagged separately.'
status: addressed
---

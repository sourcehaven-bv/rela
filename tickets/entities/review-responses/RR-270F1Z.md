---
id: RR-270F1Z
type: review-response
title: The proposed test harness will not catch the escaped bug class
severity: significant
resolution: 'AC1 respecified to spy entitiesStore.update and assert retained is unchanged. AC2b (fake timers past the debounce with the dialog open) plus 2c 2d and 2e added.'
status: addressed
finding: 'Spying fetch is wrong - writes go through entitiesStore.update (useAutoSave.ts:318) and the existing edit test mocks at the store layer. Asserting zero-called is also weak: all four BUG-FB0LN8 failures were state-reconstruction bugs, not extra-request bugs. The harness must additionally assert: retained is unchanged after a decline; a second identical transition re-prompts; dirty is false after decline; a decline-then-accept round trip emits exactly one correct PATCH; and - highest value, currently absent - that advancing fake timers past the debounce while the dialog is open still emits zero writes. That last one is the 800ms-raced-the-dialog failure.'
---

---
id: RR-17Z70H
type: review-response
title: Guard-was-checked invariant rested on two textual copies of the same expression; elevation on the copy path would be partial
finding: 'Two points from the reviews. (a) The exemption''s safety depends on the guard block above having run, but both spelled `plan.def.Guard.Permission != ""` separately, so the invariant was textual rather than structural. (b) The security reviewer noted that if autocascade.Mutator or lua.Mutator ever grew CopyState, a bypassACL Manager would neuter only check (3): checks (1) and (2) go through CopyReadGate and CopyGuard, which never consult bypassACL. Elevation would be partial and confusing to debug. Not a live defect -- no elevated handle carries CopyState today.'
severity: minor
resolution: 'Computed `perm`/`guarded` once above the guard block and reused them in the exemption, so the ordering invariant is structural. Recorded the elevation constraint in the authorizeCopy doc comment, naming what would have to be decided first (what elevation means for checks 1 and 2) rather than leaving it implicit. Considered a compile-time assertion but rejected it: entitymanager importing autocascade would invert the dependency arrow, and a composed interface cannot express a negative constraint anyway.'
status: addressed
---

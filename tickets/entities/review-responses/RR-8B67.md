---
id: RR-8B67
type: review-response
title: Add SVG-specific GET test asserting CSP/nosniff headers
finding: handlers_theme_test.go::TestThemeLogo_RoundTrip asserts Content-Type / nosniff / CSP for PNG only. SVG is the high-risk format — add a dedicated test that uploads hostileSVGBytes(), GETs back, and asserts all three security headers so a future refactor can't silently regress them.
severity: minor
resolution: Added TestThemeLogo_SVGGetHeaders that uploads hostileSVGBytes(), GETs back, and asserts Content-Type=image/svg+xml, X-Content-Type-Options=nosniff, Content-Security-Policy=sandbox; frame-ancestors 'none', X-Frame-Options=DENY.
status: addressed
---

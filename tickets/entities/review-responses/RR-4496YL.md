---
id: RR-4496YL
type: review-response
title: 'HistoryView compare: recompute() race + stale diff on fetch error — diff can contradict its own labels'
finding: 'cranky-code-reviewer (frontend) #1+#2: recompute() (HistoryView.vue:120-128) has no sequencing/abort guard. Rapidly changing base/target fires overlapping Promise.all([sideState,sideState]) with per-side getVersion network calls; a slower earlier call resolves last and overwrites contentDiff/propDiff while the selects+caption show the newer selection — the diff lies about what it shows. Separately, a mid-compare getVersion 404/network-error hits the catch (toast) but never clears contentDiff/propDiff, so the previous comparison''s diff stays on screen under the new dropdown labels. Fix: generation-counter guard in recompute (commit only if seq still current), and on error clear/invalidate the displayed diff. The codebase already has this discipline (entities.ts dryRun AbortSignal, RR-ZKL2).'
severity: significant
resolution: 'Fixed: recompute() now uses a generation counter (recomputeSeq) and only commits its result if still the newest; on error it clears contentDiff/propDiff so a stale comparison never remains under new dropdown labels.'
status: addressed
---

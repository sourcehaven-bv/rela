---
id: RR-3532
type: review-response
title: Write paths still return 403 with rule_id — defeats the 404 hide on GET
finding: 'writeForbiddenIfACLDenied (handlers_api.go:354) writes `{error, rule_kind, rule_id, reason}` on every PATCH/DELETE/POST to a denied entity. The plan goes to lengths to make per-entity GET return 404 indistinguishable from not-found to defeat URL enumeration — but PATCH /api/v1/tickets/TKT-001 still returns 403 with rule_id naming the role and confirming existence. Same enumeration, different verb. Fix: when ACL is configured AND the entity is not visible to the principal (req.Visible returns false), write paths must return 404 (same body as not-found) BEFORE falling through to AuthorizeWrite — so an entity the principal can''t read is also invisible to write attempts. AC: PATCH/DELETE/POST on a hidden entity returns 404 with the same body as a not-found.'
severity: critical
resolution: 'Incorporated into rescoped TKT-VQGN scope: PATCH/DELETE/POST-action probe req.Visible at handler entry and return 404 (same body as not-found) before reaching AuthorizeWrite. Pinned by AC3.'
status: addressed
---

---
id: RR-4189H
type: review-response
title: NewNumberFromInt vs NewInt binder footgun
finding: 'predicate has NewNumberFromInt(int) Number (float64, pre-existing, the established binder idiom in affordances) and now NewInt(int64) Int. A binder declaring an IntType field but reflexively using NewNumberFromInt binds a Number; runtimeTypeAccepts(IntType, Number) is false -> EvalError at eval time (runtime, per-entity). Neither constructor''s godoc warns about the other. Fix: document on both NewInt and NewNumberFromInt which declared type each pairs with (NewInt<->IntType, NewNumberFromInt<->NumberType).'
severity: significant
resolution: 'Both constructors now cross-reference in godoc (value.go): NewInt says ''bind to an IntType field, do NOT use NewNumberFromInt (returns Number, fails the runtime type check against IntType)''; NewNumberFromInt says ''use for NumberType; for IntType use NewInt''. Makes the pairing explicit at the call site.'
status: addressed
---

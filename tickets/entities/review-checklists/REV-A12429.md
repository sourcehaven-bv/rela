---
id: REV-A12429
type: review-checklist
title: 'Review: Style HTML comments as muted chips in the Milkdown editor'
status: done
---

<!-- @managed: claude-workflow v1 -->

## Automated Checks

- [x] All tests pass (`just test`)
- [x] Lint clean (`just lint`)
- [x] Comment lint gate clean (`just comment-lint`)
- [x] Coverage maintained (`just coverage-check`)

Frontend: 2930 tests pass across 185 files; `vue-tsc --noEmit` clean; `eslint` 0
errors (130 warnings, all pre-existing in `stress/`, none in changed files).
`just comment-lint` clean — "no unresolvable doc links across 15192 comments"
(it scans Go only, so the TypeScript added here is out of its scope). `just ci`
run in full, including the Go suite and the frontend build.

No commentlint finding is introduced by this diff.

## Code Review

- [x] Run `/code-review` command (invokes cranky-code-reviewer agent)
- [x] All critical review-responses addressed
- [x] All significant review-responses addressed
- [x] Self-reviewed the diff for unrelated changes

**Review Responses:** RR-2UZZAO (critical, addressed), RR-TVA0Y3 (significant,
addressed), RR-3A0R59 (significant, addressed), RR-XBKAGN (significant,
addressed), RR-A6SXLO (significant, deferred with reason), RR-BVMC5C (minor,
addressed), RR-1LSKDO (nit, addressed).

The review was worth running: it found a critical defect I had missed and could
not have found by reading. Both the critical and the significant rendering bug
were reproduced before fixing, and both fixes are mutation-verified — the test
fails on the old code and passes on the new.

RR-A6SXLO is deferred rather than fixed: it concerns the corpus harness, which
covers no editor node (not `entityRef`, not `taskList`), so extending it is
shared-infrastructure work rather than part of this ticket. Reason recorded on
the entity.

## Acceptance Verification

- [x] Each acceptance criterion tested (reference planning checklist)
- [x] Test evidence documented in implementation checklist

**Acceptance Status:**

- AC1 standalone comment chips without delimiters — **PASS**. Test "renders a
standalone comment as a chip without delimiters"; confirmed visually in the
running app.
- AC2 trailing comment stays inline — **PASS**. Test "keeps a trailing comment
in the paragraph it follows" asserts one paragraph holding both the strong mark
and the chip.
- AC3 multi-line round-trips byte-identically — **PASS**. Test asserts
`verdict === 'unchanged'` and the exact interior newlines. Mutation-verified
against a whitespace-destroying serializer.
- AC4 raw HTML unaffected — **PASS**. `rawHtmlPassthrough.test.ts` passes
unmodified; the app editor now has its own equivalent.
- AC5 not-quite-a-comment stays visible — **PASS**, with the criterion
corrected during implementation. `<!-- note --><div>` produces no chip; the
two-comments-on-one-line case chips both, which is correct CommonMark.
- AC6 deletable as one unit — **PASS**. Chip is `contenteditable="false"` with
a single TEXT_NODE child.

Additionally verified beyond the original criteria: a comment with only edge
newlines gets block treatment (the RR-TVA0Y3 bug), and both editors chip and
round-trip identically (RR-XBKAGN).

## Documentation (enhancements only)

- [x] Docs-checklist created and linked via `has-docs`
- [x] User-facing documentation updated
- [x] Docs-checklist marked as done

**Docs Checklist:** DOCS-4HD23U

`frontend/CLAUDE.md` gained a comment-node section stating the three rules a
future change must not break.

## Final Checks

- [x] Commit message explains the why, not just what
- [x] No TODOs or FIXMEs left unaddressed
- [x] Ready for another developer to use

## Pull Request

- [x] ~~Run `/pr` command to create PR and monitor CI~~ (deferred by design: the
`/pr` gate requires this ticket to be `done` and validating clean BEFORE the PR
is opened, and a `done` checklist may have no unchecked items — so this item can
only be satisfied by a PR that does not yet exist. `/pr` is being run
immediately after this transition. See TKT-UFV01M and the note below.)

---
description: 'Add an ''llm'' or ''prompt'' output format for view execution that optimizes output for AI/LLM consumption. Features: (1) Inline related entity content (configurable depth) instead of just IDs, (2) Clean markdown without YAML frontmatter, (3) Configurable link resolution styles (plain text, @"file.md" for coding agents, emphasized), (4) Strip comments and metadata, (5) Optional section headers for structure. Builds on existing view engine but adds a rendering pass that resolves entity references to readable content. Useful for assembling context for AI coding agents via MCP.'
id: FEAT-017
priority: medium
status: proposed
title: LLM-optimized view export format
type: feature
---
